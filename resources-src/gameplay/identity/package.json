{
  "name": "@trp/identity",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/server/main.server.ts",
  "dependencies": {
    "@trp/config": "workspace:*",
    "@trp/contracts": "workspace:*",
    "@trp/database": "workspace:*",
    "@trp/shared": "workspace:*"
  },
  "fxmanifest": {
    "dependencies": ["core-kernel"]
  },
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit"
  }
}
